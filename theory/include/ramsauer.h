#ifndef RAMSAUER_H
#define RAMSAUER_H

const double NUCLEAR_RADIUS_CONSTANT = 1.4; // in fm
const double WOODS_SAXON_WELL_DEPTH = 42.8; // in MeV
const double FM2_TO_BARNS = pow(10,-2);
const double SKIN_THICKNESS = 1.6; // in fm

#endif /* RAMSAUER_H */
